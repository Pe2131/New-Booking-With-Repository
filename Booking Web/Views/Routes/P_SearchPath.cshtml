@model List<ViewModel_PathWay>
<table class="table table-bordered table-striped table-hover js-basic-example dataTable" style="text-align:center;">
    <thead>
        <tr>
            <th>#</th>
            <th>Source</th>
            <th>Destination</th>
            <th>start Time</th>
            <th>arrive Time</th>
            <th>Price For Adult</th>
            <th>Price For Baby</th>
            <th>status</th>
            <th>Capasity</th>
            <th>Action</th>
        </tr>
    </thead>
    @if (Model.Any())
    {
        int count = 1;

        <tbody>
            @foreach (var item in Model)
            {

                <tr id="@item.id-R">
                    <th scope="row">@(count++)</th>
                    <td>@item.GetSourceName</td>
                    <td>@item.GetDestinationName</td>
                    <td>@item.StartDate</td>
                    <td>@item.ArrivalDate</td>
                    <td>@item.PriceForAdultUro € - @item.PriceForAdultDollar $ </td>
                    <td>@item.PriceForBabyUro € - @item.PriceForBabyDollar $</td>
                    <td>
                        @item.Status
                    </td>
                    <td>
                        @item.Capacity
                    </td>
                    <td>
                        <button id="@item.id" class="btn tblActnBtn" onclick="openReservForm(@item.id)">
                            <i class="material-icons">playlist_add_check</i>
                        </button>
                    </td>

                </tr>
            }

        </tbody>
    }

</table>
<script src="~/assets/js/table.min.js"></script>
<script src="~/assets/js/pages/tables/jquery-datatable.js"></script>